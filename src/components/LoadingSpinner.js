import { Spinner } from '@fluentui/react-components'
import React from 'react'

export const LoadingSpinner = () => {
    return (
        <div style={{ margin: "50%" }}>
            <Spinner />
        </div>
    )
}
